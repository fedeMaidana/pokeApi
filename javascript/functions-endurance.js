let eType1 = (data, type, text, i, element) => {
    type.style.display = 'grid';

    if(i == 0){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType2 = (data, type, text, i, element) => {
    type.style.display = 'grid';

    if(i == 1){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType3 = (data, type, text, i, element) => {
    type.style.display = 'grid';

    if(i == 2){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType4 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 2 / 4 / 3';
    type.style.justifySelf = 'center';

    if(i == 3){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType4_4 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 1 / 4 / 2';
    type.style.justifySelf = 'center';

    if(i == 3){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType4_5 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 1 / 4 / 3';
    type.style.justifySelf = 'center';

    if(i == 3){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType4_7 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 2 / 3 / 3';
    type.style.justifySelf = 'center';

    if(i == 3){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType4_9 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 1 / 3 / 2';
    type.style.justifySelf = 'center';

    if(i == 3){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType4_10 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 1 / 3 / 2';
    type.style.justifySelf = 'center';

    if(i == 3){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType5 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 2 / 4 / 4';
    type.style.justifySelf = 'center';

    if(i == 4){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType5_5 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 2 / 4 / 3';
    type.style.justifySelf = 'center';

    if(i == 4){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType5_7 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 3 / 3 / 4';
    type.style.justifySelf = 'center';

    if(i == 4){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType5_9 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 2 / 3 / 3';
    type.style.justifySelf = 'center';

    if(i == 4){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType5_10 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 2 / 3 / 3';
    type.style.justifySelf = 'center';

    if(i == 4){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType6 = (data, type, text, i, element) => {
    type.style.display = 'grid';

    if(i == 5){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType6_6 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 3 / 4 / 4'

    if(i == 5){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType6_9 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 3 / 3 / 4';
    type.style.justifySelf = 'center';

    if(i == 5){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType6_10 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 3 / 3 / 4'

    if(i == 5){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType7 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '3 / 1 / 4 / 2';

    if(i == 6){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType7_7 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '3 / 1 / 4 / 4';

    if(i == 6){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType7_8 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '3 / 1 / 4 / 3';
    type.style.justifySelf = 'center';

    if(i == 6){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType8 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '3 / 1 / 4 / 4';

    if(i == 7){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType8_8 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '3 / 2 / 4 / 4';
    type.style.justifySelf = 'center';

    if(i == 7){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType9 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '3 / 3 / 4 / 4';

    if(i == 8){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType10 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '4 / 1 / 5 / 2';

    if(i == 9){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType10_10 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '4 / 1 / 5 / 4';
    type.style.justifySelf = 'center';

    if(i == 9){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType10_11 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '4 / 1 / 5 / 3';
    type.style.justifySelf = 'center';

    if(i == 9){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let eType11 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '4 / 2 / 5 / 4';

    if(i == 10){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}