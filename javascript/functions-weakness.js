let wType1 = (data, type, text, i, element) => {
    type.style.display = 'grid';

    if(i == 0){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let wType2 = (data, type, text, i, element) => {
    type.style.display = 'grid';

    if(i == 1){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let wType3 = (data, type, text, i, element) => {
    type.style.display = 'grid';

    if(i == 2){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let wType4 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 2 / 4 / 3';
    type.style.justifySelf = 'center';

    if(i == 3){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let wType4_4 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 1 / 4 / 2';
    type.style.justifySelf = 'center';

    if(i == 3){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let wType4_5 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 1 / 4 / 3';
    type.style.justifySelf = 'center';

    if(i == 3){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let wType4_7 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 1 / 3 / 2';
    type.style.justifySelf = 'center';

    if(i == 3){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let wType5 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 2 / 4 / 4';
    type.style.justifySelf = 'center';

    if(i == 4){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let wType5_5 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 2 / 4 / 3';
    type.style.justifySelf = 'center';

    if(i == 4){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let wType5_7 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 2 / 3 / 3';
    type.style.justifySelf = 'center';

    if(i == 4){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let wType6 = (data, type, text, i, element) => {
    type.style.display = 'grid';

    if(i == 5){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let wType6_6 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 3 / 4 / 4'

    if(i == 5){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let wType6_7 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '2 / 3 / 3 / 4'

    if(i == 5){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}

let wType7 = (data, type, text, i, element) => {
    type.style.display = 'grid';
    type.style.gridArea = '3 / 1 / 4 / 4';

    if(i == 6){
        type.style.backgroundColor = pokeTypeColor(element.name);

        position = data.names.findIndex(info => info.language.name === 'es');
        text.innerHTML = data.names[position].name;
    }
}